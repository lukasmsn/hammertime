## Design Guidelines (App Behavior)

- Do not block UI for network calls; show optimistic UI for workout finish then sync in background.
- If Supabase config is missing, app silently continues local-only.
